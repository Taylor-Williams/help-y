<% if @post.user.id == current_user %>
  <%= render 'form' %>
<% else %>
  <h3><%= @post.title %></h3><br>
  <div><%= @post.content %></div>
  <%= link_to "See author's page", user_path(@post.user) %>
<% end %>