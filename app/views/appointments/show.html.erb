<%= render partial: 'appointments/display_info', locals: { appointment: @appointment } %>
<% if %>
  <%= render 'volunteers/signup' if @appointment.available? || @appointment.has_user?(current_user) %>
<% end %>