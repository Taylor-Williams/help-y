<%= form_for([@post, appointment]) do |f| %>
  <%= f.hidden_field :user_id, :value => current_user %>
  <%= f.hidden_field :post_id, :value => @post.id %>
  <%= f.text_area :info, size: "100x10" %>
  <%= f.submit %>
<% end %>